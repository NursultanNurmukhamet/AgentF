[project]
name = "ai_mid_agent"
version = "0.6.0"
requires-python = ">=3.10"
dependencies = [
    "aws-opentelemetry-distro",
    "bedrock-agentcore==1.22.0",
    "boto3>=1.40.0",
    "strands-agents>=1.15.0",
]
